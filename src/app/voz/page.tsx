// Página dedicada para hablar por voz con el asistente (Retell web call).
// Pensada para abrirse desde un enlace en móvil (p. ej. enviado por WhatsApp):
// pantalla completa enfocada solo en el micrófono y la UX de marcar / colgar.
import VoiceCallScreen from "@/components/VoiceCallScreen";

export default function VozPage() {
  return <VoiceCallScreen />;
}
